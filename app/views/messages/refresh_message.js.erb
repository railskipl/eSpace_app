<% unless @total_messages.empty? %>

  jQuery('#refresh_msg')
  .html('<%= escape_javascript(render "refresh_message", :total_messages => @total_messages) %>')

  jQuery(".content").mCustomScrollbar("scrollTo", "bottom");
<% end %>
