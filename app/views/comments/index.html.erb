<div class="comments">
<%= render 'comments',  :comments => @comments  %>
</div>

<div class="more-new"> <%= link_to 'More', comments_path(:post_id => params[:post_id], :page => @comments.next_page), :class => 'load-more-posts', :remote => true if @comments.next_page %> </div>
