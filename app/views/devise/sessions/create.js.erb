<% if current_user.present? %>
  location.reload();
<% else %>
  jQuery.facebox('<%= escape_javascript(render :template => 'devise/sessions/new.html') %>');
  jQuery('#facebox form').data('remote','true');
<% end %>