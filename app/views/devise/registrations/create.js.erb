<% if @resource_saved %>
  location.reload();
<% else %>
  jQuery.facebox('<%= escape_javascript(render :template => 'devise/registrations/new.html') %>');
  jQuery('#facebox form').data('remote','true');
<% end %>