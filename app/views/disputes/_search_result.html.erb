<tr>

<td><%= index + 1 %></td>
<td><%= booking.id %></td>
<td><%= booking.post_id %></td>
<td><%= booking.area %></td>
<td> 
	<%= link_to "#{booking.poster_name} #{booking.poster_last_name}", dispute_path(booking) %>
</td>
<td>
	<%= link_to "#{booking.user_name} #{booking.user_last_name}", dispute_path(booking) %>
</td>
<td><%= booking.post_address %> </td>
<td>
	<%= link_to "Hold", hold_money_dispute_path(booking, :search=> params[:search]) %> | <%= link_to "View Booking", booking_path(booking), :target=>"_blank"  %>

</td>
</tr>